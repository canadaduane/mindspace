{
  "name": "mindspace",
  "version": "1.0.0",
  "description": "An experiment in spatial notes",
  "scripts": {
    "dev": "echo 'Open http://localhost:8080/src/ (note trailing slash)' && sirv --no-clear --dev",
    "build": "esbuild --bundle src/main.js --format=esm --minify --external:colorjs.io >dist/bundle.js"
  },
  "keywords": [],
  "author": "Duane Johnson <duane.johnson@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@b9g/crank": "0.5.4",
    "@fontsource/yantramanav": "5.0.15",
    "colorjs.io": "0.4.5",
    "nanoid": "5.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.4",
    "flow-bin": "^0.220.1",
    "sirv-cli": "^2.0.2"
  }
}
